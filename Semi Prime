
    
    
           *****************************
                       OR
           *****************************


from Prime import is_prime
import math
n=int(input())
x=int(math.sqrt(n))
for i in range(2,x+1):
    if is_prime(i):
        if n%i==0:
            y=n//i
            if is_prime(y):
                print("Semi prime")
                break
else:
    print("Not a Semi prime")
